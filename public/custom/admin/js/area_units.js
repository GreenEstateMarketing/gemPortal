$(document).ready(function(){
    /////////////////////////forumlaphp artisan cms:theme:assets:publish update for area units///
    /* $("#area_units").change(function () {
         unit=$(this).val();
        var marla_to_sq= <?php echo setting('real_estate_marla_to_square_meter');?>;
        var sqft_to_sq= <?php echo setting('real_estate_sqfeet_to_square_meter');?>;
        area_unit_value=$("#area_unit_value").val();
        if(unit=="m2")
        {
            $("#square").val(area_unit_value);
        }
        else if(unit=="ft2")
        {
            var calc=(area_unit_value/sqft_to_sq);
            res=(parseFloat(calc).toFixed(2));
            $("#square").val(res);
        }
        else if(unit=="ma")
        {
            calc=(area_unit_value*marla_to_sq);
            res=(parseFloat(calc).toFixed(2));
            $("#square").val(res);
        }
        else{}
     });
     $("#area_unit_value").change(function () {
         var marla_to_sq= <?php echo setting('real_estate_marla_to_square_meter');?>;
         var sqft_to_sq= <?php echo setting('real_estate_sqfeet_to_square_meter');?>;
         area_unit_value=$(this).val();
         unit=$("#area_units").val();
         if(unit=="m2")
         {
             $("#square").val(area_unit_value);
         }
         else if(unit=="ft2")
         {
             var calc=(area_unit_value/10.764);
             res=(parseFloat(calc).toFixed(2));

             $("#square").val(res);
         }
         else if(unit=="ma")
         {
             calc=(area_unit_value*20.90);
             res=(parseFloat(calc).toFixed(2));
             $("#square").val(res);
         }
         else{}
     });*/
});

